# Scraper 抓取监控与预警设计文档

## 1. 系统目标
- 实时监控 Scraper 异步抓取任务。
- 深度分析任务结果，识别潜在失败或数据异常。
- 自动告警，在任务异常或成功率下降时及时推送至钉钉群。
- 可扩展性，支持多爬虫类型和多任务并行监控。

## 2. 关键模块设计

### 2.1 任务创建模块
- 功能：调用 `/tasks-create` 创建抓取任务。
- 输出：`task_id` 用于轮询和结果分析。
- 可扩展：支持不同爬虫参数和文件命名模板。

### 2.2 状态轮询模块
- 功能：定期调用 `/tasks-status` 查询任务状态。
- 状态分类：
  - `Running` → 继续轮询
  - `Ready` → 进入结果分析
  - `Failed` → 触发严重告警
- 配置：轮询间隔 `POLL_INTERVAL` 可调，超时机制触发告警。

### 2.3 结果分析模块
- 功能：获取任务列表 `/tasks-list` 或下载文件 `/tasks-download`。
- 关键指标：`success_rate`, `error_number`, `file_size`, `api_code`, `api_error_msg`
- 规则：
  - 成功率 < 0.95 → 警告
  - 成功率 < 0.8 → 严重警告
  - 错误次数 > 0 → 警告
  - 文件大小异常 → 警告
  - API 回调错误 → 警告

### 2.4 异常检测模块
- 功能：综合状态、成功率、错误次数、文件大小判断任务健康。
- 健康评分（0~100），低于阈值触发警报。
- 分级示例：≥80 正常，50~79 警告，<50 严重警告。

### 2.5 钉钉告警模块
- 功能：将异常信息发送至钉钉群。
- 消息模板：
```
[级别] Scraper任务异常预警
任务 ID: {task_id}
网站: {spider_name}
爬虫: {spider_id}
状态: {status}
成功率: {success_rate}
错误次数: {error_number}
文件大小: {file_size} bytes
API 回调: {api_code} {api_error_msg}
建议操作: {操作建议}
```
- 可扩展：支持多群组告警或不同级别分发。

### 2.6 日志与存储模块
- 功能：保存任务历史状态和指标。
- 数据：任务 ID、创建时间、完成时间、成功率、错误次数、文件大小。
- 可扩展：接入数据库，绘制趋势图。

## 3. 数据流设计
```
任务创建 -> task_id -> 状态轮询 -> Ready/Failed -> 结果分析 -> 异常检测 -> 钉钉告警 -> 日志存储
```

## 4. 任务轮询与告警策略
### 4.1 状态异常
- 状态 `Failed` → 严重告警
- 状态长时间 `Running` → 超时告警

### 4.2 成功率异常
- 成功率 < 0.95 → 警告
- 成功率 < 0.8 → 严重警告
- 连续下降趋势 → 趋势告警

### 4.3 数据完整性
- 文件大小异常 → 警告
- 错误次数 > 0 → 警告
- API 回调异常 → 警告

### 4.4 综合健康评分
- 结合状态、成功率、错误次数和文件大小
- 分级：≥80 正常，50~79 警告，<50 严重警告

## 5. 可配置项
| 配置 | 说明 |
|------|------|
| POLL_INTERVAL | 任务轮询周期（秒） |
| SUCCESS_RATE_THRESHOLD | 警告成功率阈值 |
| SEVERE_RATE_THRESHOLD | 严重警告成功率阈值 |
| DINGTALK_WEBHOOK | 钉钉机器人 Webhook |
| ALERT_TEMPLATE | 警告消息模板 |
| MAX_RUNNING_TIME | 任务最长允许运行时间 |

## 6. 可扩展性设计
1. 多任务并行轮询
2. 多爬虫类型注册
3. 告警策略灵活化
4. 可视化趋势分析

## 7. 技术选型建议
| 功能 | 技术选型 |
|------|----------|
| 异步轮询 | asyncio / aiohttp / Celery |
| 数据存储 | PostgreSQL / MySQL |
| 告警推送 | 钉钉 Webhook |
| 可视化 | Grafana / 简易前端 |
| 配置管理 | YAML / JSON |

## 8. 下一步执行计划
1. 明确常用爬虫列表及参数格式
2. 配置钉钉 Webhook
3. 确定轮询周期和成功率阈值
4. 实现基础监控脚本验证流程
5. 扩展多任务并行、趋势分析和